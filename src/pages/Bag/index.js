import React from "react";
import * as RN from 'react-native';
import BageContent from './BageContent'

const BagScreen = () => {

  return (
    <RN.SafeAreaView>
      <BageContent />
    </RN.SafeAreaView>
  );
};

export default BagScreen;